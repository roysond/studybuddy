# StudyBuddy backend environment variables
# Copy values into your shell profile, launchSettings, or a local .env loader.
# Never commit real secrets.

OPENROUTER_API_KEY=
ConnectionStrings__DefaultConnection=
